What URL should be used to access your application?
localhost:8888. Directory path changed to "/public/frontend-consume-api-Rohan" .

What libraries did you use to write your application?
AngularJs
Bootstrap
jQuery

What influenced the design of your user interface?
I used Bootstrap to design the page and that influenced the designs.

What steps did you take to make your application user friendly?
I used Bootstrap libraries to display beautiful look and feel to the user interfaces and used AngularJs to GET and POST the data for APIs.

What steps did you take to insure your application was secure?
I used Cookie, localStorage and server side Global Variable(session) to secure the application.

What could be done to the front end or back end to make it more secure?
-- We can check that the GET or POST request should be coming from a live user, not from Robots.
-- We can use Postman package in backend to authenticate every request coming from the front end.
-- We can add constraints in frontend to prevent Automatic login/API accessing from auto bots.
